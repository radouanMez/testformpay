// app.partners.tsx
import {
    Page
} from "@shopify/polaris";

export default function PartnersSettings() {
    return (
        <Page
            title="Visibility"
            primaryAction={{ content: "Save Changes" }}
            secondaryActions={[{ content: "Cancel" }]}
        >
           
        </Page>
    );
}